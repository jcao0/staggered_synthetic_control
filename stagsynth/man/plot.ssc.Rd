% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.ssc}
\alias{plot.ssc}
\title{Plot Event-Time ATT from SSC Estimation}
\usage{
\method{plot}{ssc}(
  x,
  main = "Event-time ATT (SSC)",
  xlab = "Event time",
  ylab = "ATT estimate",
  ci = !anyNA(x$ci_lower_event),
  ...
)
}
\arguments{
\item{x}{An object of class \code{"ssc"}.}

\item{main}{Title string.}

\item{xlab, ylab}{Axis labels.}

\item{ci}{Logical: draw the confidence band?  Default \code{TRUE} if
inference is available.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A \code{ggplot} object (invisibly) if \pkg{ggplot2} is available;
  otherwise a base-R plot is drawn and \code{NULL} is returned invisibly.
}
\description{
Plot Event-Time ATT from SSC Estimation
}
\examples{
set.seed(1)
N <- 10; Ttot <- 8
Y <- matrix(rnorm(N * Ttot), N, Ttot)
D <- matrix(0L, N, Ttot)
D[1:3, 5:Ttot] <- 1L   # units 1-3 treated from period 5
fit <- ssc(Y, D, S = 2, alpha = 0.05)
plot(fit)

}
